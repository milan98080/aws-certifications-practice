# Production Environment Configuration

# Database Configuration (Update with your AWS RDS details)
DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/aws_practice

# Database SSL Configuration (required for RDS)
DB_SSL_REJECT_UNAUTHORIZED=false

# JWT Configuration
JWT_SECRET=your-production-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=https://aws.milan-pokhrel.com.np

# API Base URL for production
API_BASE_URL=https://api.milan-pokhrel.com.np